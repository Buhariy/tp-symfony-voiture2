{% extends "front/front_base.html.twig" %}


{% block body %}
<div class="container">
<h1>{{vehicule.titre}}</h1>
<img src="{{ asset("media/" ~ vehicule.photo) }}" alt="Véhicule" width="500px">
<p>{{vehicule.description}}</p><a class="btn btn-success" href="{{ path('front_show_vehicule', {'id' : vehicule.id}) }}">Je loue ce véhicule</a></a>
</div>
{% endblock %}